---
description: TitanGold Rule 02 — C1 Deployment-Gate Syntax Blocker Correction
alwaysApply: true
---

# TitanGold Current Active Work (Rule 02)

Current outcome:
**PRODUCTION RESOURCE STABILIZATION —
C1 DEPLOYMENT-GATE SYNTAX BLOCKER CORRECTION**

Current area:
Telegram ingestion / collector runtime

Current program slice:
RESOURCE-STABILIZATION-WP-C-C1

Risk context:
Tier 3 program / source+CI correction only

Final review state:
**PR #29 SOURCE MERGE = COMPLETE**
**C1 PRODUCTION DEPLOYMENT ATTEMPT = STOPPED BEFORE INSTALL**
**PRODUCTION C1 FILES = NOT DEPLOYED**
**PRODUCTION COLLECTOR = PRE-C1 / UNCHANGED**
**DEPLOYMENT BLOCKER = canonical channelPollingService.js syntax invalid**
**C1 PRODUCTION DEPLOY = NOT AUTHORIZED**
**C1 PRODUCTION STABILIZATION = NOT YET VERIFIED**
**C1 REAL WORKING = NO / NOT YET PROVEN**
**C2 DB/INDEX = DEFERRED / NOT AUTHORIZED**
**B10 050 = BLOCKED_BY_PRODUCTION_RESOURCE_STABILIZATION**
**B10 051 = NOT AUTHORIZED**
**B10 RUNTIME WIRING = NOT AUTHORIZED**
**SHADOW / WP-D / PAPER / LIVE = NOT STARTED**

Canonical product main baseline:
`origin/main` = `b74fb74fdc0c7d9757023e98f95b61ccf2f4c2ea`

Current branch / worktree:
- Branch: `fix/c1-runtime-syntax-gate`
- Worktree: `/home/ubuntu/worktrees/titangold-c1-syntax-fix`

Deployment blocker:
A block comment in `telegram-collector/dist/services/channelPollingService.js`
contained `src/**/*`. The embedded `*/` terminated the JavaScript block comment
early, so `node --check` failed with `SyntaxError: Unexpected token '*'`.
The Owner-authorized targeted C1 production deployment correctly stopped
before install. Production target files remained pre-C1 / unchanged.
No PM2 restart. Production DB mutation = 0.

Authorized now:
- exact syntax-only comment correction
- CI runtime syntax gate for the exact five-file C1 deployment surface
- Rule02 transition
- focused source validation
- commit/push branch
- Draft PR
- GitHub Actions

Not authorized:
- production deployment
- PM2 mutation
- DB / index / C2
- migrations
- B10 050 / B10 051
- runtime wiring
- Shadow / Paper / Live

Accepted C1 guarantees (unchanged):
- Bounded collector concurrency (`TELEGRAM_POLL_CONCURRENCY`, default 3, min 1, max 10)
- Polling-cycle overlap prevented (`isRunning` fail-closed)
- Initial connection attempt max 1 (`connectionRetries: 1`)
- Immediate application retry = 0
- Client reuse per session identity per cycle
- Assigned-account identity isolation fail-closed
- Deterministic cleanup
- DB query semantics unchanged (`MAX(message_id)`, saveMessages)
- No DB schema change

Residual risk / NOT FIXED:
- GramJS 2.26.22 all-path reconnect behavior
- Possible destroy/reconnect race
- Possible dependency-owned reconnect after lifecycle termination
- `MAX(message_id)` DB access path
- Production DB index requirement / C2

THIS SYNTAX FIX DOES NOT AUTHORIZE PRODUCTION DEPLOYMENT.
THIS SYNTAX FIX DOES NOT UNBLOCK B10 050.
C1 DOES NOT CLAIM ALL GRAMJS INTERNAL RECONNECT PATHS ARE CONTROLLED.

Next Owner gate after successful CI:
**C1 SYNTAX-FIX PR FINAL REVIEW**

Do not merge from this Rule 02.
Do not deploy from this Rule 02.
Do not start C2 in this Rule 02.
