---
description: TitanGold dynamic active-work authority — post-merge scheduler incident closeout
alwaysApply: true
---

# TitanGold Current Active Work (Rule 02)

Current outcome:
Close the post-merge Scheduler incident, land Scheduler-safe backend deployment
guards, and complete post-merge Human QA for authentication and Agents/Arbitrage
browser navigation on main @ `2e5e493`.

Current area:
Deployment safety · Scheduler ownership · Post-merge verification

Current program slice:
POST-MERGE-SCHEDULER-INCIDENT — Remediation + Closeout

Risk tier:
Tier 3 — Scheduler/runtime safety remediation

Engineering status:
HOTFIX IN REVIEW — `fix/postmerge-scheduler-deploy-guard`

Human-QA status:
PENDING — user must confirm clean-browser login/logout/re-login cycle

Expected baseline:
- Main merge baseline: `2e5e493676f223836a910a0ee57c8197afeef00b`
- Runtime marker: `2e5e493`
- Scheduler owner: `titan-engine-worker` (single owner, do not restart)
- Scheduler allowlist: `["arbitrage"]` only — do not expand
- Demo + Emergency Stop remain active; Live impossible

Authorized scope:
- PM2 deployment guard hardening (`titan-backend` only)
- Deployment regression tests (mocked PM2 harness)
- Scheduler recovery verification (read-only, no worker restart)
- Post-merge browser smoke evidence
- Canonical Rule 02 update for incident closeout

Hard boundaries:
- Do not start the next Agent outcome
- Do not call MEXC or access exchange credentials
- Do not execute financial actions
- Do not expand Scheduler allowlist
- Do not restart or delete `titan-engine-worker`
- Do not deploy guard by manually editing runtime files
- Do not merge hotfix PR without review

Freeze status (pending incident closeout PASS):
- Agents Platform canonical registry — **CLOSED pending scheduler-safety PR**
- Arbitrage read-only workflow — **CLOSED pending scheduler-safety PR**
- Authentication/CORS deployment regression — **CLOSED**
- Deployment environment guard — **ACTIVE BASELINE** (being extended with PM2 guard)

Documented limitations:
- Invalid direct Agent UI route uses safe SPA redirect (API fail-closed preserved)
- Only Arbitrage is allowlisted; other Agents remain Limited
- Tier-4 / Live blocked

Acceptance criteria:
- RCA for accidental Scheduler restart documented
- Backend deploy cannot target Scheduler or wildcard PM2 verbs
- Guard tests pass (`failed=0`, `environment-blocked=0`)
- Scheduler owner=1, allowlist unchanged, 3 normal ticks verified
- User confirms post-merge login/logout/re-login
- Actual browser navigation smoke for Agents + Arbitrage (EN/FA)
- Hotfix PR open to main (not merged)

Next transition:
**READY FOR SCHEDULER-SAFETY PR REVIEW** → merge hotfix → resume Agents/Arbitrage freeze closeout
