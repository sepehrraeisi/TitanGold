---
description: TitanGold Rule 02 — C1 Deployment-Gate Source Correction Delivery
alwaysApply: true
---

# TitanGold Current Active Work (Rule 02)

Current outcome:
**PRODUCTION RESOURCE STABILIZATION —
C1 DEPLOYMENT-GATE SOURCE CORRECTION DELIVERY**

Current area:
Telegram ingestion / collector runtime

Current program slice:
RESOURCE-STABILIZATION-WP-C-C1

Risk context:
Tier 3 program

Final review state:
**PR #29 C1 SOURCE MERGE = COMPLETE**
**FIRST C1 PRODUCTION DEPLOYMENT ATTEMPT = STOPPED BEFORE INSTALL**
**PRODUCTION COLLECTOR = PRE-C1 / UNCHANGED**
**SOURCE SYNTAX BLOCKER = FIXED**
**C1 DEPLOYMENT-SURFACE SYNTAX CI = ENFORCED**
**PR #30 FINAL REVIEW = PASS**
**PR #30 = OWNER APPROVED FOR SQUASH MERGE**
**C1 PRODUCTION DEPLOY = NOT AUTHORIZED BY THIS RULE02**
**C1 PRODUCTION VERIFICATION = NOT YET PASS**
**C1 REAL WORKING = NO / NOT YET PROVEN**
**C2 DB/INDEX = DEFERRED / NOT AUTHORIZED**
**B10 050 = BLOCKED_BY_PRODUCTION_RESOURCE_STABILIZATION**
**B10 051 = NOT AUTHORIZED**
**B10 RUNTIME WIRING = NOT AUTHORIZED**
**SHADOW / WP-D / PAPER / LIVE = NOT STARTED**

Canonical product main baseline before this syntax-fix merge:
`origin/main` = `b74fb74fdc0c7d9757023e98f95b61ccf2f4c2ea`

Accepted technical head:
`e52286dad3df26b46fa245fd8574d253afa5a539`

Current branch / worktree:
- Branch: `fix/c1-runtime-syntax-gate`
- Worktree: `/home/ubuntu/worktrees/titangold-c1-syntax-fix`

Deployment-gate history:
The first Owner-authorized targeted C1 production deployment correctly stopped
before install because canonical `channelPollingService.js` contained a
block-comment lexical syntax defect (`src/**/*` embeds `*/`). Production
target files remained pre-C1 / unchanged. No PM2 restart. Production DB
mutation = 0. The syntax defect is now fixed by comment wording only.
Executable runtime semantics are unchanged.

Deployment-gate improvement:
The exact five C1 runtime deployment files are now syntax-validated by CI using
`node --check` before unit tests.

Accepted C1 source guarantees:
- Bounded collector concurrency (`TELEGRAM_POLL_CONCURRENCY`, default 3, min 1, max 10)
- Polling-cycle overlap prevented (`isRunning` fail-closed)
- Initial connection attempt max 1 (`connectionRetries: 1`)
- Immediate application retry = 0
- Client reuse per session identity per cycle
- Assigned-account identity isolation fail-closed
- Deterministic cleanup
- DB query semantics unchanged (`MAX(message_id)`, saveMessages)
- No DB schema change

Residual risk / NOT FIXED:
- GramJS 2.26.22 internal reconnect not fully controllable
- Possible destroy/reconnect dependency race
- Possible dependency-owned reconnect after lifecycle termination
- `MAX(message_id)` inefficient access path
- Required DB access-path/index work deferred to C2

MERGING PR #30 DOES NOT DEPLOY C1 TO PRODUCTION.
MERGING PR #30 DOES NOT UNBLOCK B10 050.
RESOURCE STABILIZATION IS NOT COMPLETE.
C1 DOES NOT CLAIM ALL GRAMJS INTERNAL RECONNECT PATHS ARE CONTROLLED.

Not authorized by this Rule02:
- production deployment
- PM2 mutation
- DB / index / C2
- migrations
- B10 050 / B10 051
- runtime wiring
- Shadow / Paper / Live

When PR #30 lands on main:

Next Owner gate:
**C1 TARGETED PRODUCTION DEPLOYMENT RETRY
+ STABILIZATION VERIFICATION**

Do not authorize that deployment from this Rule 02.
Do not start C2 in this Rule 02.
