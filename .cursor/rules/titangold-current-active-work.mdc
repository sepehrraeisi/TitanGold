---
description: TitanGold Rule 02 — Production Resource Stabilization C2 Source Delivery
alwaysApply: true
---

# TitanGold Current Active Work (Rule 02)

Current outcome:
**PRODUCTION RESOURCE STABILIZATION —
C2 SOURCE/CI VERIFIED / PRODUCTION EXECUTION NEXT GATE**

Current area:
Telegram ingestion + PostgreSQL telegram_messages access path

Current program slice:
RESOURCE-STABILIZATION-WP-C-C2

Risk context:
Tier 3

## Durable provenance

C2 accepted technical PR head:
`07488458437c171eeba30ae08e4c4aa01a6e8887`

C2 accepted migration SHA256:
`2c7c5723b20fd823102fb78f8cd97664453e3fcbb07ece56efb4ec0bd444f082`

C1 deployed technical source baseline:
`2f8ea66301e3ca392b1d3182622da6bac9c1c4fc`

Do not treat the squash-merge SHA as the C2 technical tree identity.
The accepted PR head above remains the reviewed technical baseline.

## State

**C1 PRODUCTION = VERIFIED**
**C1 RESOURCE EFFECT = IMPROVED**
**C1 REAL WORKING = YES — COLLECTOR-SIDE SCOPE ONLY**
**RESOURCE STABILIZATION COMPLETE = NO**
**C2 SOURCE IMPLEMENTATION = FINAL REVIEW PASS**
**C2 CI = PASS**
**C2 PRODUCTION EXECUTION = NOT AUTHORIZED BY SOURCE MERGE**
**B10 050 = BLOCKED_BY_PRODUCTION_RESOURCE_STABILIZATION**
**B10 051 = NOT AUTHORIZED**
**B10 RUNTIME WIRING = NOT AUTHORIZED**
**SHADOW / WP-D / PAPER / LIVE = NOT STARTED**

C1 REAL WORKING = YES does NOT mean RESOURCE_STABILIZATION_COMPLETE = YES.

## C2 source (locked)

Migration:
`052_telegram_messages_channel_message_id_index`

Index:
`idx_telegram_messages_channel_message_id`
NON-UNIQUE BTREE on `public.telegram_messages (channel_id, message_id)`

- UNIQUE = NO
- DESC = NO
- INCLUDE = NO
- predicate = NO
- IF NOT EXISTS = NO
- PRODUCTION BUILD MODE = CREATE INDEX CONCURRENTLY
- TRANSACTION = DISABLED FOR 052 (`pgm.noTransaction()` on up and down)

PROCESS_DEVIATION = YES (backend-tests.yml and e2e-tests.yml disposable
`telegram_messages` fixtures). Owner accepted those two existing patches
on technical head `0748845...`. No further technical expansion.

## Production execution architecture (NOT executed by this merge)

**STRATEGY E-HARDENED** =
canonical reviewed 052
+ exact SHA gate
+ local node-pg-migrate 8.0.4
+ one-file isolated migration directory
+ named-file selector

Normal production `npm run migrate:up` remains **BLOCKED / MUST NOT BE USED FOR C2**
because the canonical directory still contains pending 050 and 051.

Do not use `npx`, `--fake`, or `--no-lock`.
Isolated `.js` staging requires a parent `package.json` `{"type":"module"}`
outside the one-file migrations dir (loader context, not a migration).

**MERGING C2 SOURCE DOES NOT EXECUTE 052.**
**MERGING C2 SOURCE DOES NOT CREATE THE INDEX IN PRODUCTION.**
**MERGING C2 SOURCE DOES NOT UNBLOCK B10 050.**
RESOURCE STABILIZATION REMAINS INCOMPLETE UNTIL POST-C2 PRODUCTION VERIFICATION.

## 050 / 051 / residual

**050 = BLOCKED / UNCHANGED / NOT RECONCILED**
**051 = NOT AUTHORIZED**
**GRAMJS INTERNAL RECONNECT = RESIDUAL / OUT OF C2 SCOPE**

Post-C1 `load1 < nproc` samples do NOT unblock 050.

## Next Owner gate

**C2 PRODUCTION EXECUTION
FINAL SAFETY + LIVE PREFLIGHT + BUILD/VERIFY AUTHORIZATION**

This is NOT automatic execution.
It requires a separate Owner prompt with fresh production evidence.
