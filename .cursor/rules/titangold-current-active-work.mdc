---
description: TitanGold Rule 02 — Production Resource Stabilization C2 source + CI
alwaysApply: true
---

# TitanGold Current Active Work (Rule 02)

Current outcome:
**PRODUCTION RESOURCE STABILIZATION —
C2 DB ACCESS-PATH SOURCE + CI IMPLEMENTATION**

Current area:
Telegram ingestion + PostgreSQL telegram_messages access path

Current program slice:
RESOURCE-STABILIZATION-WP-C-C2

Risk context:
Tier 3

Canonical main baseline:
`d29a3b63e1096f62d1e509f3d0f8bda2684f41d3`

C1 deployed technical source baseline:
`2f8ea66301e3ca392b1d3182622da6bac9c1c4fc`

State:
**C1 SOURCE = MERGED**
**C1 PRODUCTION VERIFICATION = PASS**
**C1 RESOURCE EFFECT = IMPROVED**
**C1 REAL WORKING = YES — FOR THE AUTHORIZED COLLECTOR-SIDE SCOPE ONLY**
**RESOURCE STABILIZATION COMPLETE = NO**
**FULL SERVER / MAIN ALIGNMENT = NO**
**C2 SOURCE + CI = AUTHORIZED ON THIS BRANCH**
**C2 PRODUCTION EXECUTION = NOT AUTHORIZED**
**B10 050 = BLOCKED_BY_PRODUCTION_RESOURCE_STABILIZATION**
**B10 051 = NOT AUTHORIZED**
**B10 RUNTIME WIRING = NOT AUTHORIZED**
**SHADOW / WP-D / PAPER / LIVE = NOT STARTED**

C1 REAL WORKING = YES does NOT mean RESOURCE_STABILIZATION_COMPLETE = YES.

## C2 accepted design (locked)

Index:
`idx_telegram_messages_channel_message_id`
on `public.telegram_messages (channel_id, message_id)`

- UNIQUE = NO
- DESC = NO
- INCLUDE = NO
- predicate = NO
- IF NOT EXISTS = NO
- PRODUCTION BUILD MODE = CREATE INDEX CONCURRENTLY
- numeric slot = 052
- language = JavaScript / ESM
- `pgm.noTransaction()` required on up and down

DESC is unnecessary because PostgreSQL 14 btree can backward-scan for MAX.
Non-unique because uniqueness is already enforced by
`idx_telegram_messages_unique (message_id, channel_id)`.

## Authorized on this branch

- canonical C2 migration source
- fail-closed same-name / definition / invalid-index preconditions
- PostgreSQL 14 disposable proof
- CI workflow support
- read-only / disposable CI verifier
- Draft PR

## NOT authorized

- production C2 execution
- production DB mutation
- CREATE INDEX / DROP INDEX on production
- pgmigrations production mutation
- `npm run migrate:up` on production
- migration 050 execution or ledger reconciliation
- migration 051 execution
- PM2 mutation
- server alignment
- B10 050
- B10 051
- GramJS private hook

## Future production execution architecture (NOT executed here)

**STRATEGY E-HARDENED** =
canonical numbered 052
+ exact named-file selector
+ isolated temp migration directory containing ONLY C2

Normal production `npm run migrate:up` remains **UNSAFE / BLOCKED**
because the canonical directory still contains pending 050 and 051.

Do not use `--fake` or `--no-lock`.
Do not use npx for future production execution.
Use the hash/version-gated local `backend/node_modules/.bin/node-pg-migrate`
(must remain 8.0.4). Isolated `.js` staging requires a parent
`package.json` `{"type":"module"}` outside the one-file migrations dir
so Node loads ESM; that file is loader context, not a migration.

## Residual / not C2 scope

**GRAMJS INTERNAL RECONNECT = RESIDUAL DEPENDENCY RISK / NOT C2 SCOPE**

050 ledger/schema divergence remains a separate Owner gate.
051 remains not authorized.

Post-C1 `load1 < nproc` samples do NOT unblock 050.

## Next Owner gate after successful Draft PR

**C2 SOURCE/CI FINAL REVIEW**

Not production execution.
Not merge.
Not 050 retry.
