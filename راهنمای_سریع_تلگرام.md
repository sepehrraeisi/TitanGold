# 🚀 راهنمای سریع: اتصال واقعی Telegram Collector

**تاریخ**: ۱۴۰۳/۰۹/۰۶  
**زمان لازم**: ۱۰-۱۵ دقیقه  
**وضعیت**: ✅ آماده راه‌اندازی

---

## 📋 خلاصه اقدامات

برای فعال‌سازی اتصال واقعی به تلگرام (بدون داده Mock):

```
1️⃣ دریافت اعتبارنامه API از my.telegram.org
2️⃣ پیکربندی فایل .env
3️⃣ ری‌استارت سرویس
4️⃣ ورود از طریق فرانت‌اند
```

---

## 🔑 مرحله 1: دریافت اعتبارنامه Telegram API

### روش سریع:
1. **باز کردن**: https://my.telegram.org
2. **ورود**: با شماره تلفن (مثال: `+989123456789`)
3. **تایید**: کد تایید از اپلیکیشن تلگرام
4. **کلیک**: روی "API development tools"
5. **ایجاد**: برنامه جدید (App title: `TitanGold`)
6. **کپی**: `api_id` و `api_hash`

### مثال خروجی:
```
api_id: 1234567
api_hash: abcdef1234567890abcdef1234567890
```

**⚠️ مهم**: این اطلاعات را **محرمانه** نگه دارید!

---

## ⚙️ مرحله 2: پیکربندی

### دستور سریع:
```bash
cd /home/ubuntu/webapp/TitanGold/telegram-collector
nano .env
```

### محتوای فایل:
```bash
PORT=3002

# اعتبارنامه‌های شما:
TELEGRAM_API_ID=1234567                          # جایگزین کنید
TELEGRAM_API_HASH=abcdef1234567890abcdef1234567890  # جایگزین کنید
TELEGRAM_PHONE_NUMBER=+989123456789              # شماره خودتان

# فقط اگر 2FA دارید:
TELEGRAM_PASSWORD=رمز_عبور_2FA                   # اختیاری

CACHE_TTL=300
```

**ذخیره**: `Ctrl+X` → `Y` → `Enter`

---

## 🔄 مرحله 3: ری‌استارت سرویس

### دستورات:
```bash
# ری‌استارت
pm2 restart telegram-collector

# بررسی وضعیت
curl http://localhost:3002/health
```

### خروجی مورد انتظار:
```json
{
  "status": "healthy",
  "configured": {
    "apiId": true,   ← باید true باشد
    "apiHash": true, ← باید true باشد
    "session": false ← بعد از ورود true می‌شود
  }
}
```

---

## 🌐 مرحله 4: ورود از فرانت‌اند

### مراحل در مرورگر:
1. **باز کردن**: http://188.40.209.82:3000
2. **رفتن به**: AI Center → Data Hub → Telegram Collector
3. **وارد کردن**: شماره تلفن (`+989123456789`)
4. **کلیک**: "Send Verification Code" ✅
5. **دریافت**: کد 5 رقمی از تلگرام
6. **وارد کردن**: کد و (در صورت نیاز) رمز 2FA
7. **کلیک**: "Confirm Login"

### نتیجه موفق:
```
✅ Login successful
✅ Session saved
✅ Ready to track channels
```

---

## 📊 مرحله 5: ردیابی کانال‌ها

### افزودن کانال:
1. رفتن به تب **"Channel Management"**
2. وارد کردن نام کاربری کانال: `crypto` (بدون @)
3. کلیک روی **"Add Channel"**

### مشاهده پیام‌ها:
1. رفتن به **"Tracked Channels"**
2. انتخاب کانال از لیست
3. مشاهده پیام‌های اخیر

---

## 🔧 عیب‌یابی سریع

### مشکل: "Failed to send verification code"
```bash
# بررسی logs
pm2 logs telegram-collector --lines 50

# بررسی پیکربندی
cat /home/ubuntu/webapp/TitanGold/telegram-collector/.env | grep -v "^#"
```

### مشکل: "Invalid verification code"
- کد منقضی شده (5 دقیقه اعتبار دارد)
- کد جدید درخواست کنید

### مشکل: "2FA password required"
- رمز 2FA را در فرانت‌اند وارد کنید
- یا به `.env` اضافه کنید

---

## ✅ چک‌لیست موفقیت

پس از تکمیل، بررسی کنید:

```
✅ اعتبارنامه از my.telegram.org دریافت شد
✅ فایل .env پیکربندی شد
✅ سرویس ری‌استارت شد
✅ Health check: apiId: true, apiHash: true
✅ ورود از فرانت‌اند موفق بود
✅ Session ذخیره شد
✅ امکان ردیابی کانال‌ها وجود دارد
```

---

## 🎯 دستورات مفید

### بررسی وضعیت:
```bash
pm2 status telegram-collector
pm2 logs telegram-collector
curl http://localhost:3002/health
```

### ری‌استارت:
```bash
pm2 restart telegram-collector
```

### تست Health:
```bash
curl http://localhost:3000/api/telegram-collector/health
```

---

## 📚 مستندات کامل

برای جزئیات بیشتر:
- `TELEGRAM_API_SETUP_GUIDE.md` - راهنمای کامل (انگلیسی)
- `TELEGRAM_COLLECTOR_SETUP.md` - راهنمای راه‌اندازی (فارسی)
- `TELEGRAM_COLLECTOR_PROXY_FIX.md` - جزئیات Proxy

---

## 🎉 تبریک!

پس از تکمیل این مراحل:

```
✅ Telegram Collector با داده‌های واقعی کار می‌کند
✅ دیگر هیچ داده Mock وجود ندارد
✅ می‌توانید کانال‌های تلگرام را ردیابی کنید
✅ پیام‌ها به‌صورت واقعی دریافت می‌شوند
✅ آماده استفاده در تحلیل و Autopilot
```

---

## 🔗 لینک‌های مفید

| منبع | آدرس |
|------|------|
| **دریافت API** | https://my.telegram.org |
| **Frontend** | http://188.40.209.82:3000 |
| **Health Check** | http://188.40.209.82:3000/api/telegram-collector/health |
| **GitHub** | https://github.com/sepehrraeisi/TitanGold |

---

## 💡 نکات مهم

1. **محرمانگی**: `api_id` و `api_hash` را با کسی به اشتراک نگذارید
2. **Session**: بعد از اولین ورود، دیگر نیازی به ورود مجدد نیست
3. **Cache**: پیام‌ها برای 5 دقیقه کش می‌شوند (سرعت بیشتر)
4. **امنیت**: فایل `.env` در `.gitignore` است و به GitHub push نمی‌شود

---

## 📞 پشتیبانی

اگر مشکلی پیش آمد:

```bash
# لاگ‌های کامل
pm2 logs telegram-collector --lines 100

# وضعیت سرویس
pm2 status

# تست اتصال
curl http://localhost:3002/health
```

---

**ایجاد شده**: ۱۴۰۳/۰۹/۰۶  
**وضعیت**: ✅ آماده استفاده  
**سطح سختی**: آسان  

**🚀 TitanGold آماده اتصال به داده‌های واقعی تلگرام است!**
