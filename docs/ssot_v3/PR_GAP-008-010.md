# Pull Request: DataHub Sources + Categories backend-first

## Branch

`feat/gap-008-sources-backend-wiring`

## Create PR (GitHub CLI)

```bash
# One-time: authenticate (token needs repo scope)
gh auth login -h github.com

# Create PR
gh pr create \
  --base main \
  --head feat/gap-008-sources-backend-wiring \
  --title "DataHub: backend-first Sources (GAP-008) and Categories (GAP-010)" \
  --body-file docs/ssot_v3/PR_GAP-008-010.md
```

## Compare URL (if CLI auth unavailable)

https://github.com/sepehrraeisi/TitanGold/compare/main...feat/gap-008-sources-backend-wiring?expand=1

## Summary

- **GAP-008 (Closed)**: Sources tab wired to `/api/v1/data-sources` (GET/POST/PUT/DELETE/PATCH).
- **GAP-010 (Closed)**: Categories tab wired to `/api/v1/data-categories` (GET/POST/PUT/DELETE).
- **GAP-009 / GAP-011 (Open)**: RBAC role checks remain separate; wiring is Implemented without RBAC.

## Test plan

- [ ] Sources: Network shows `GET/POST/PUT/DELETE/PATCH` on `/api/v1/data-sources`
- [ ] Categories: Network shows `GET/POST/PUT/DELETE` on `/api/v1/data-categories`
- [ ] No `fetchDataHubState` on Sources/Categories CRUD paths
- [ ] `npm run build`
