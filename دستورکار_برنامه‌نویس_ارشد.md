# دستورکار برنامه‌نویس ارشد – Data Hub و Telegram Collector

این سند خلاصهٔ وضعیت فعلی و کارهایی است که باید انجام دهی (اجرا، تست، دپلو، نگهداری).

---

## ۱. وضعیت فعلی

- **بک‌لاگ Telegram Collector (TASK-TC-001 تا TASK-TC-015)** انجام شده است.
- سورس ورودی کلکتور بازگردانده شده: `telegram-collector/src/index.ts` → بیلد → `dist/index.js` + `dist/index.legacy.js`.
- Nginx برای `/api/telegram-collector` تنظیم شده؛ URL کلکتور در فرانت با `api.buildCollectorUrl()` یکپارچه است.
- Job زمان‌بندی‌شده برای انتقال پیام‌های تلگرام به pipeline در بک‌اند وجود دارد (هر ۵ دقیقه، قابل تنظیم با `TELEGRAM_PIPELINE_INTERVAL_MS`).
- تست یکپارچگی برای روت‌های telegram در `backend/__tests__/integration/telegramCollector.test.js` اضافه شده.

**تسک اختیاری باقی‌مانده:** TASK-TC-016 (چند کانال per DataSource) در بک‌لاگ؛ در صورت نیاز بعداً انجام شود.

---

## ۲. چه کار کنی

### ۲.۱ توسعه (Development)

برای دیدن UI و تست Telegram Collector سه سرویس را هم‌زمان اجرا کن:

| سرویس              | پورت | دستور |
|--------------------|------|--------|
| Backend            | 5002 | `cd backend && npm run dev` |
| Telegram Collector | 3002 | `cd telegram-collector && npm run start` (یا `npm run dev`) |
| Frontend (Vite)    | 3000 | `cd /home/ubuntu/webapp/TitanGold && npm run dev` |

مرورگر: **http://localhost:3000** → AI → Manage AI → Data Hub → **Telegram Collector**.

جزئیات کامل در **`BUILD_AND_DEPLOY.md`** (§۲).

### ۲.۲ بیلد و اجرای کلکتور

```bash
cd /home/ubuntu/webapp/TitanGold/telegram-collector
npm install
npm run build    # در صورت تغییر src/index.ts
npm run start    # یا در production با PM2
```

سرویس اصلی کلکتور همان **root** پروژه است (`telegram-collector/dist`). پوشه‌های `deploy/blue/telegram-collector` و `deploy/green/telegram-collector` نسخهٔ ساده‌تر هستند؛ در production از root اجرا کن (مستند در BUILD_AND_DEPLOY §۲.۲).

### ۲.۳ تست سلامت و روتینگ

- سلامت کلکتور: `GET http://localhost:3002/health`
- از پشت Nginx (در صورت تنظیم): `GET http://<host>/api/telegram-collector/health`
- تست یکپارچگی بک‌اند (وقتی Jest در PATH باشد): از پوشهٔ backend اجرای تست‌های integration (فایل بالا).

### ۲.۴ Deploy و Production

- راهنمای کامل: **`DEPLOY.md`** (در صورت وجود) یا **`BUILD_AND_DEPLOY.md`**.
- بعد از بیلد فرانت: `npm run build` در root؛ Nginx از `dist` سرو می‌کند؛ در صورت نیاز `nginx -s reload` و restart سرویس‌ها (مثلاً PM2).
- متغیر build-time برای URL کلکتور: **`VITE_TELEGRAM_COLLECTOR_URL`** (اختیاری؛ مستند در BUILD_AND_DEPLOY §۳.۱).

### ۲.۵ نگهداری و عیب‌یابی

- **معماری و روت‌ها:** **`TELEGRAM_COLLECTOR_ANALYSIS.md`** (نقشهٔ کامپوننت‌ها، جریان داده، لیست روت‌های کلکتور و بک‌اند).
- **طراحی UI Data Hub:** **`DESIGN_SYSTEM_DATAHUB.md`**.
- **تست E2E و سناریوها:** **`TELEGRAM_E2E_TEST_PLAN.md`**.
- سیاست امنیتی کلکتور: API کلکتور فقط از پشت Nginx و داخلی است؛ در صورت در معرض قرار گرفتن، auth یا IP allowlist اضافه کن (مستند در تحلیل §۱.۲).

---

## ۳. فایل‌ها و مراجع کلیدی

| موضوع              | فایل / مسیر |
|--------------------|-------------|
| راهنمای بیلد و اجرا | `BUILD_AND_DEPLOY.md` |
| معماری و بک‌لاگ TC | `TELEGRAM_COLLECTOR_ANALYSIS.md` |
| طراحی Data Hub     | `DESIGN_SYSTEM_DATAHUB.md` |
| ورودی کلکتور      | `telegram-collector/src/index.ts` |
| اپ اصلی کلکتور     | `telegram-collector/dist/index.legacy.js` |
| روت‌های data-sources (telegram) | `backend/routes/data-sources.js` |
| سینک و pipeline   | `backend/services/telegramSync.js`, `telegramPipeline.js` |
| زمان‌بندی انتقال پیام | `backend/engine/scheduler.js` (startTelegramPipelineScheduler) |
| Nginx              | `infrastructure/nginx.conf`, `deploy/blue|green/infrastructure/nginx.conf` |
| تست integration   | `backend/__tests__/integration/telegramCollector.test.js` |

---

## ۴. خلاصه

- تغییرات **اعمال شده** و از نظر کد و روتینگ **آماده** هستند.
- برای **قابل رویت** بودن در محیط خودت: Backend + Telegram Collector + Frontend را اجرا کن و از مسیر Data Hub → Telegram Collector استفاده کن.
- برای **production**: طبق BUILD_AND_DEPLOY و DEPLOY بیلد کن، Nginx و PM2 را تنظیم و reload/restart کن.
- در صورت نیاز به جزئیات معماری یا تسک‌های بعدی، به **TELEGRAM_COLLECTOR_ANALYSIS.md** و **DATAHUB_TELEGRAM_TASKS.md** مراجعه کن.
